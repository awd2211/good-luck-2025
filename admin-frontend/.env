# 默认开发环境配置
# 开发环境: npm run dev (自动使用 .env.development)
# 生产环境: npm run build (自动使用 .env.production)
# 直接连接后端，后端已配置CORS
# 使用localhost而非0.0.0.0，因为前端需要从浏览器访问后端
VITE_API_BASE_URL=http://localhost:50301/api/manage
